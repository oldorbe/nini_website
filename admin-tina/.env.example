# GitHub: repo that contains content/ (same repo when using admin-tina in subdirectory)
GITHUB_OWNER=
GITHUB_REPO=
GITHUB_BRANCH=main

# GitHub Personal Access Token with repo read/write
GITHUB_PERSONAL_ACCESS_TOKEN=

# NextAuth.js (required for production)
NEXTAUTH_SECRET=

# Vercel KV (required for production self-hosted backend)
KV_REST_API_URL=
KV_REST_API_TOKEN=
